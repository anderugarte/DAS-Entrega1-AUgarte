<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.entrega1_das">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_filmometry"
        android:label="Filmometry"
        android:roundIcon="@mipmap/ic_filmometry_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistroInicioSesion.InicioSesion">
        </activity>
        <activity android:name=".RegistroInicioSesion.Registro" />
        <activity android:name=".Principal.MenuPrincipal" />
        <activity android:name=".RegistroInicioSesion.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>